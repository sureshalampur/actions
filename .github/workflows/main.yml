name: First Action file
on: workflow_dispatch
jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: fun
        run: |
          echo "Hi Suresh"   
          echo "How are you?"

  second:
    runs-on: ubuntu-latest
    needs: first
    steps:
      - name: for fun
        run: echo "Iam Good {{vars.NAME}}"
  
